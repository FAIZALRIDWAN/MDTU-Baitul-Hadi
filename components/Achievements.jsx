'use client'

import { Trophy, Users, GraduationCap, Award } from 'lucide-react'

const achievements = [
  
]

export default function Achievements() {
  return (
    <section id="prestasi" className="py-16 md:py-24 bg-gradient-to-br from-blue-50 to-indigo-50">
    </section>
  )
}
